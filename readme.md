url: https://burhankapadia18.github.io/portfolio/

static site, no build step. open `index.html` or serve the folder.

- `styles/tokens.css` design tokens (light + dark)
- `styles/global.css` shared chrome
- `styles/home.css`, `styles/article.css` page styles
- `scripts/site.js` theme toggle + scroll reveal
- `DESIGN.md` the design language, in the same schema as `design-md/`
